<%
'Dim theDate As Date
'theDate = Format(theDate, "yyyy")
'arrParts = Split(strOldFormat, ".")
'theDate = DateTime.DateSerial(parts(2), parts(1), parts(0))
'strNewFormat = Format(theDate, "mm.dd.yyyy")
Dim theDate
theDate = formatdatetime(now(),4)
%>